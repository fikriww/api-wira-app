services:
  api:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: api-wira-app
    restart: unless-stopped
    environment:
      - PORT=8000
    ports:
      - "127.0.0.1:8000:8000"
